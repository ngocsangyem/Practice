var name = 'Sang';
var age = 20;
// age < 20 ? console.log('Water') : console.log('Beer');


///////////////

var job = 'Structure';

switch (job){
	case 'Student':
		return console.log(name + ' is student' );
	case 'Teacher':
		return console.log(name + ' is teacher' );
	case 'Driver':
		return console.log(name + ' is driver' );
	default:
		return console.log(name + ' is homeless');
}

